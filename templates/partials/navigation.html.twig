{% if app.user %}
        <div class="pb-5">
            <nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
                <div class="container-fluid">
                    <a class="navbar-brand" href="{{ path('app_home') }}">
                        <img src="{{ asset('assets/images/Gamesplanner-logo.png') }}" 
                            alt="GammesPlanner Logo"
                            height="40"
                        >
                    </a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarColor03">
                        <ul class="navbar-nav ms-auto">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('availability_calendar') }}">Dostępność</a>
                            </li>
                            {% if is_granted('ROLE_DM') %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('games') }}">Sesje</a>
                            </li>
                            {% endif %}
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                                    <i class="bi bi-person-circle"></i>
                                </a>
                                <div class="dropdown-menu dropdown-menu-end">
                                    <a class="dropdown-item" href="{{ path('app_user_panel') }}"><i class="bi bi-person-gear"></i> Ustawienia</a>
                                    {% if is_granted('ROLE_ADMIN') %}
                                    <a class="dropdown-item" href="{{ path('admin_users') }}"><i class="bi bi-gear-wide-connected"></i> Panel</a>
                                    {% endif %}
                                    <div class="dropdown-divider"></div>
                                    <a class="dropdown-item text-danger" href="{{ path('app_logout') }}"><i class="bi bi-power"></i> Logout</a>
                                </div>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>
        </div>
{% endif %}